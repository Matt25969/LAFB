apiVersion: v1
kind: Pod
metadata:
  name: db-connector
  labels:
    app: db_connector
spec:
  containers:
    - name: db-connector
      image: teamdeadweight/db_connector:latest
      ports:
      - containerPort: 5001
