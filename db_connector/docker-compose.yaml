version: '3.7'
services:
  app:
    container_name: connector
    restart: always
    build: .
    ports:
      - '5001:5001'
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'
